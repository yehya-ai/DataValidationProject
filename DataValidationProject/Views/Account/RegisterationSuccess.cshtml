@* Views/Account/RegistrationSuccess.cshtml *@
@{
    ViewData["Title"] = "Registration Successful";
}

<div class="container">
    <div class="alert alert-success mt-4">
        <h4 class="alert-heading">Registration Successful!</h4>
        <p>Your form was submitted successfully because all validation rules passed.</p>
        <hr>
        <p class="mb-0">The validation attributes on the UserRegistration model ensured that:</p>
        <ul>
            <li>Username followed the required pattern and was not in our "taken" list</li>
            <li>Email address was in a valid format</liss
            <li>Password met complexity requirements</li>
            <li>Password and confirmation password matched</li>
            <li>Phone number was in a valid format</li>
        </ul>
    </div>

    <div class="mt-3">
        <a asp-controller="Account" asp-action="Register" class="btn btn-outline-success me-2">Try Registration Again</a>
        <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary">Back to Demos</a>
    </div>
</div>
